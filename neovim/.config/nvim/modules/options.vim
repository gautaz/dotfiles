"ensure diffs are side by side
set diffopt+=vertical

"foresee next lines when going up or down
set scrolloff=7

"switch from unsaved buffers and do not abandon unsaved buffers without
"confirmation
set hidden
set confirm

"avoid wildmenu to select first entry automatically
set wildmode=longest,list
